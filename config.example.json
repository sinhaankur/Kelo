{
  "finnhubApiKey": "YOUR-FINNHUB-KEY",
  "llmEndpoint": "http://localhost:11434",
  "llmModel": "qwen2.5:7b",
  "cashAvailable": 2500,
  "_note": "Copy to config.json (gitignored). finnhubApiKey powers market headlines in the sentiment card; llm settings are used by pulse-tui --analyze; cashAvailable feeds the Trade Draft card's affordability math (Pulse drafts orders, never places them)."
}
